<a href="/event">Events Dashboard</a> <a href="/logout">Logout</a>
<h1>Edit Your Profile</h1>

<%= form_for @user, url:'/user/'+current_user.id.to_s do |f| %>
	<input name="_method" type="hidden" value="patch" />
	<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
	First Name: <%= f.text_field :first_name %> <br>
	Last Name: <%= f.text_field :last_name %><br>
	Email: <%= f.text_field :email %><br>
	Location: <%= f.text_field :location_city %> <%= f.select :location_state, options_for_select([['AL', 'AL'],['AK', 'AK'],['AZ', 'AZ'],['AR', 'AR'],['CA', 'CA'], ['CO', 'CO'], ['CT', 'CT']]) %><br>
	<%= f.submit "Update Records" %>
<% end %>

